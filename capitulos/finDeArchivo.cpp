#include<bits/stdc++.h> // Incluir todas las bibliotecas estándar necesarias

using namespace std;

int main() {
    double costo; // Declarar una variable para almacenar el costo

    // Leer continuamente valores de costo hasta que no se puedan leer más
    while (cin >> costo) {
        cout << costo << endl; // Imprimir el costo leído
    }

    return 0;
}
